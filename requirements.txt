# ============================================================
# Content Factory — зависимости проекта
# Установка: pip install -r requirements.txt
# ============================================================

# --- Telegram ---
python-telegram-bot==20.7  # Бот: публикует посты, принимает команды, слушает channel_post

# --- AI / Генерация контента ---
anthropic>=0.25.0          # Claude API — генерация текстов постов

# --- Планировщик задач ---
apscheduler==3.10.4        # Запуск постера и генератора по расписанию

# --- RSS парсер ---
feedparser==6.0.11         # Парсинг RSS-лент для инфоповодов
beautifulsoup4==4.12.3     # Извлечение картинок из HTML описания статей
lxml==5.2.2                # Быстрый HTML/XML парсер (используется BeautifulSoup)

# --- HTTP клиент (для image_fetcher и WB-парсера) ---
aiohttp==3.9.5             # Асинхронные запросы к Unsplash и Pexels API
aiohttp-socks==0.8.4       # SOCKS5-прокси поддержка для aiohttp
curl_cffi>=0.7.0           # Chrome TLS fingerprint — обход PoW-защиты WB

# --- Утилиты ---
python-dotenv==1.0.1       # Загрузка .env файла

# --- Логирование ---
loguru==0.7.2              # Удобные логи вместо стандартного logging
openai>=1.90.0             # OpenAI API - optional GPT provider for text generation/analyze/rephrase
