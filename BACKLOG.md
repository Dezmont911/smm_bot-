# BACKLOG — smm_bot

Живой план работ. Приоритеты сверху вниз. Отмечай `[x]` по мере выполнения.
Контекст, архитектура и конвенции — в `CLAUDE.md` (единственный авторитет по состоянию).
Постановки SaaS/расходов — в `SAAS_SPEC.md` / `COST_SPEC.md`.

Легенда статусов карты V2: ✅ есть · 🟡 частично · ❌ нет.

> **Сделано в сессии (сверх плана):** персистентное РСЯ-перекрытие (переживает рестарт),
> плавающие слоты (MIN_GAP=40, пропуск налезающего слота), глобальные запретки, сокращённый
> `/add` (3 шага), новый экран «Мои каналы» (поиск/удалённые/пагинация), картинка по всему
> посту, дедуп референсов + кликабельные ссылки + фильтр Max, round-robin доноров,
> защита планировщика от рестартов.

---

## Карта архитектуры V2 → текущее состояние

Вывод: бóльшая часть V2 уже реализована — но как **модули в монолите**, а не отдельные
сервисы (микросервисы под 40–100 каналов = оверинжиниринг). V2 = добить 4 недостающих
куска + причесать границы, а не строить заново.

| Компонент V2 | Статус | Где / что осталось |
|---|---|---|
| Topic Service | ✅ | `_collect_topics`: search→RSS→web(+авто-дискавери лент)→evergreen→синтез-резерв |
| Topic Memory (embeddings) | ✅ | `dedup.py` + `_get_used_topics` (вкл. skipped) + `_is_duplicate`; ⚠️ без прунинга окна |
| Channel DNA | ✅ (лёгкая) | `archetypes`+`content_router`+поля карточки; тон единый — намеренно не раздуваем |
| Content Service | ✅ | `ai_client.generate_post` |
| Image Service | 🟡 | `image_fetcher`+`image_generator`, запрос из текста поста; **кэш запросов — нет** |
| Quality Check | 🟡 | длина/отказ/дубль/used/reddit-фильтр есть; **явный слой + 1 регенерация — формализовать**; LLM-судья НЕ нужен |
| Buffer Service | ✅ | `buffer_manager` |
| Publisher Service | ✅ | `poster` (без генерации при постинге); ⚠️ **идемпотентность — нет** |
| 🔸 Scope-гейт релевантности | ❌ | из критики V2 — главный фикс дрейфа тем |
| 🔸 Пуллинг тем по нишам | ❌ | из аудита — главный рычаг экономии LLM |
| 🔸 Учёт расходов | ❌ | из аудита — бюджет в приоритете |
| 🔸 Лимитер LLM/Telegram + джиттер | ❌ | из аудита — анти-flood и анти-«стадо» |

---

## Tier 0 — гигиена и хвосты ✅ ЗАКРЫТО

- [x] `git init` + первый коммит.
- [x] `CLAUDE.md`: предохранитель буфера = авто-дискавери `web_scraper`, синтез-резерв — страховка.
- [x] **relay-референсы end-to-end** — фото/альбомы протестированы вживую, file_id-публикация без лимита 50 МБ.
- [x] Чистка зависших `awaiting_media` — ежечасно (:15) + на старте (реконсиляция сирот).
- [x] Альбомы (grouped media) — **склеиваем в один media_group** (подпись на первом, лимит 10).

### Чек-лист приёмки relay-референсов (Tier 0) — ✅ ВСЁ ЗАКРЫТО (проверено вживую)
- [x] `read_candidates` отдаёт `media_kind` + режет негодные по лимитам в `skipped`.
- [x] При импорте: текст → `ready`; медиа → `awaiting_media`.
- [x] `forward_to_bot` пересылает медиа в ЛС бота (server-side, без скачивания).
- [x] Хендлер ловит форвард, достаёт `file_id`, матчит по `ref:донор:msg_id` → `ready`.
- [x] Публикация `send_*(file_id)` БЕЗ шапки «переслано».
- [x] Большое видео (>50 МБ) через `file_id` без ошибок лимита.
- [x] Защищённый донор (`ChatForwardsRestrictedError`) — пропуск с алёртом.
- [x] Юзербот НЕ требуется в админах целевого канала.

---

## Tier 1 — качество контента (главная боль из логов + критика V2)

- [x] **Эмбеддинг-гейт релевантности темы** (он же «Topic Rating»): косинус кандидат-темы
      к профилю канала, отсев off-topic ДО генерации (`_filter_relevant`, `RELEVANCE_MIN=0.28`,
      in-scope fallback `max(3,count//2)`). ✅ СДЕЛАНО.
- [ ] **Источник под тип канала**: релиз → референсы; новости → search; вечнозелёное →
      evergreen/RSS. Сделать осознанным выбором/подсказкой при `/add`, а не ручной правкой.
- [ ] **Quality Check как явный слой**: длина + запретные слова + дубль + отказ
      (детерминированно) + максимум **1** регенерация при провале. Куски есть — собрать.

## Tier 1.5 — SaaS / мультитенант (закрытый тест по инвайтам) → подробно в `SAAS_SPEC.md`

Цель: пустить тестеров по инвайт-коду на месяц, потом ограничить; тестеры НЕ видят
каналы/данные владельца; критические алерты летят владельцу в ЛС.

- [x] **Фаза 1 — изоляция + инвайты:** `owner_id` в карточках (+миграция existing→ADMIN_CHAT_ID
      выполнена, 18 каналов), таблицы `users`/`invite_codes`, `accounts.py` (роли/инвайты/планы),
      `/start` по коду и deep-link `?start=<code>`, `assert_owns()` в колбэках/командах + гард в
      `ui_router`, фильтрация листингов по owner, админ-команды `/gen_invite`/`/users`/`/grant`/`/revoke`.
      Смоук на VPS зелёный (изоляция: владелец 18 / тестер 0). ⚠️ Алёрты с контекстом тестера — Фаза 3.
- [~] **Фаза 2 — лимиты + капы бюджета:** ✅ семафор на юзербот (глоб. `asyncio.Lock` +
      FloodWait-backoff в `userbot_reader._userbot_op`). ❌ `entitlements.py` (планы trial/free/pro),
      ❌ проверки в точках входа (add/bulk/schedule/референсы/ai/web-search), ❌ таблица `usage`
      + дневные капы LLM/FLUX на юзера (защита бюджета — **приоритет №1, не сделано**).
- [~] **Фаза 3:** ✅ админ-команды `/gen_invite`/`/users`/`/grant`/`/revoke` (сделаны в Фазе 1).
      🟡 даунгрейд триала: логика `effective_plan→free` есть, ❌ джоба паузы сверх-квотных каналов.
      ❌ алёрты владельцу с контекстом тестера (`[owner_id @канал]`), ❌ `/feedback`.

> **Сделано сверх Tier 1.5 (эти сессии):** ИИ-тема вместо фритекста (убрана инъекция через тему),
> усиленная анти-мета/запретка по границе слова, кап генерации 10/прогон, ручной /add
> (автоназвание + тема из анализа), серверный hardening (fail2ban + sshd MaxStartups),
> мягкая обработка `Forbidden`/сетевых ошибок в `error_handler`.

## Tier 2 — масштаб и бюджет (40–100 каналов)

- [ ] **Пуллинг тем по нишам**: один web-search на нишу → раздать N каналам. Главный
      выигрыш по LLM-расходам.
- [ ] **Учёт расходов** → подробно в `COST_SPEC.md`: точный учёт по токенам (Claude) + FLUX
      в `usage_costs`, кнопка **«💰 Расходы»** в меню (ТОЛЬКО админ): Claude/FLUX/сток за
      сегодня/7/30 дней + фикс. (прокси/VPS) справочно. Один учёт кормит и капы тестеров,
      и личный дашборд + дневной порог/алерт.
- [~] **Лимитер LLM/Telegram** (семафор): ✅ семафор юзербота сделан; ❌ лимитер LLM-вызовов
      и джиттер публикаций внутри часа — пока нет.
- [x] **`/bulk_add`** — массовое добавление списком (до 20 за раз, авто-анализ каждого).
- [ ] **Папки/группы каналов** + групповые действия.

## Tier 3 — управление и надёжность

- [ ] Режим модерации на канал (авто-публикация vs ревью).
- [x] **Ручные посты / смешанный режим** — черновики (текст/фото/видео/альбомы в очередь
      по одному/все), правка текста/медиа, фокус-карточка ревью.
- [ ] Идемпотентность публикации (анти-двойной-постинг при падении между send и mark_published).

## Tier 4 — позже / по триггеру (не раньше, чем упрётся)

- [ ] Postgres — когда запись SQLite станет узким местом (`DATABASE_URL` готов).
- [ ] Векторный индекс (sqlite-vss) + прунинг Topic Memory — когда линейный косинус замедлится.
- [ ] Дедуп: ререрайт вместо skip; межканальный дедуп.
- [ ] `tv_series/fandom` архетип; альбомы референсов (если не закрыли в Tier 0).
- [ ] Image Service: кэш поисковых запросов/результатов картинок.
- [ ] Аналитика просмотров → `format_bias`, дашборд расходов, веб-панель, кросс-постинг,
      A/B, командные роли, авто-вступление, РСЯ-мониторинг.

---

## Что НЕ делать сейчас (из аудита)

Не дробить на микросервисы, не тащить Redis/Celery, не вводить LLM-судью качества,
не оптимизировать по метрикам (аналитики пока нет). Всё это преждевременно при текущих
объёмах — одна машина перевозит с запасом.
